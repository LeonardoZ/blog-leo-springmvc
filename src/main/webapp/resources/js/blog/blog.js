$(function(){
	/**
	function configuraComprimentoDosPosts(){
		var $posts = $(".post-content");
		$.each($posts, function(index, item){
			var $postContent = $(item);
			var content = $postContent.text();
			content = content.substring(0,800);
			content += "...";
			$postContent.text(content);
		});
		
	}
	
	configuraComprimentoDosPosts();
	**/
});